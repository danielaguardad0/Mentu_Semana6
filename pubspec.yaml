name: mentu_app
description: "Aplicación móvil educativa para organización académica y gestión de tutorías"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  # 🎯 FIREBASE AUTENTICACIÓN (NUEVO)
  firebase_core: ^2.24.2 # Versión reciente
  firebase_auth: ^4.15.2 # Versión reciente

  # State Management
  flutter_riverpod: ^2.5.1 # Versión actualizada

  # CONEXIÓN BACKEND (Para futuras Tareas/API propia)
  dio: ^5.4.1

  # UI Components
  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0

  # Charts and Calendar
  table_calendar: ^3.0.9
  fl_chart: ^1.1.1
  cloud_firestore: ^4.17.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/